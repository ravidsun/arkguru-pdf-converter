# Local wizard API. Phase pipelines stay in their own repos; this package
# only needs a web server plus the env/config helpers used to wrap them.
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.0
python-dotenv>=1.0
pyyaml>=6.0
httpx>=0.27
pytest>=7.0
